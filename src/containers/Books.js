import React from 'react';
import { View, Text } from 'react-native';

export default function Books({ words }) {
  return (
    <View>
      {/* {words.map((w, index) => (
        <Text key={`${w}${index}`} style={{ color: 'black' }}>
          {w.wordName}
          {w.partOfSpeech}
        </Text>
      ))} */}
      <Text>blank</Text>
    </View>
  );
}
